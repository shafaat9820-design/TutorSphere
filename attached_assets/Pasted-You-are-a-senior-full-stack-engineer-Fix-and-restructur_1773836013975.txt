You are a senior full-stack engineer. Fix and restructure my entire project so it runs perfectly in VS Code without any configuration issues.

CURRENT PROBLEM:

* Project exported from Replit is using pnpm workspace (monorepo)
* Environment variables (.env) are not loading correctly
* DATABASE_URL errors are occurring
* Rollup/Vite frontend errors are happening
* ES module issues like "__dirname not defined"
* Package resolution issues like "@workspace/db" not found
* dotenv not working properly across packages

GOAL:
Convert this project into a SIMPLE, SINGLE-FOLDER FULLSTACK APP that runs easily in VS Code using npm (not pnpm).

REQUIREMENTS:

1. REMOVE MONOREPO COMPLEXITY

* Remove pnpm workspace
* Remove multiple packages like @workspace/db, api-server, etc.
* Merge everything into ONE project structure:

  * /backend
  * /frontend

2. BACKEND SETUP (Node.js + Express)

* Use Express.js
* Use dotenv properly (single .env at root)
* Use PostgreSQL with direct connection (no workspace packages)
* Remove drizzle-kit complexity if causing issues OR configure it properly
* Ensure this works with:
  npm install
  npm run dev

3. DATABASE

* Use simple PostgreSQL connection using pg
* Read DATABASE_URL from .env correctly
* No path issues
* No __dirname issues
* No workspace imports

4. FRONTEND

* Fix Vite config
* Remove rollup native dependency issues
* Ensure it runs with:
  npm run dev

5. ENVIRONMENT VARIABLES

* Use only ONE .env file at root
* Ensure dotenv works without any manual path hacks
* No multiple .env files

6. PACKAGE MANAGER

* Replace pnpm with npm
* Remove pnpm-lock.yaml
* Remove workspace configs

7. SCRIPTS
   Root package.json should have:

* "dev" → run backend + frontend together
* "server" → backend only
* "client" → frontend only

8. CLEAN CODE

* Remove all unused configs
* Remove broken imports like "@workspace/*"
* Replace with relative imports

9. FINAL RESULT
   After setup, project should run with:
10. npm install
11. npm run dev

WITHOUT ANY ERRORS.

IMPORTANT:

* Do NOT leave any broken imports
* Do NOT require manual fixes after export
* Everything must work out-of-the-box in VS Code

Return:

* Clean folder structure
* Updated package.json
* Fixed backend code
* Fixed frontend config
* Working database connection
* .env example file
