You are an expert MERN stack developer.

Fix and refactor my existing project so that it becomes a clean, standard Node.js + Express + React (Vite) application that can run locally in VS Code without errors.

IMPORTANT: My current project was generated using Replit AI and contains broken imports, monorepo-style aliases (like @workspace/*), and invalid dependencies. These must be removed or replaced.

---

GOALS:

1. Remove all Replit-specific code and dependencies:

   * Remove all imports like "@workspace/*"
   * Replace them with proper relative imports
   * Remove "catalog:" and "workspace:" dependencies

2. Fix backend (Node.js + Express):

   * Convert to a clean Express server
   * Ensure entry file is src/index.ts or index.js
   * Fix all routes to use valid imports
   * Ensure server runs with:
     npm run dev

3. Fix database setup:

   * If drizzle ORM is used, configure it properly
     OR
   * Replace with a simple MongoDB (Mongoose) setup if easier
   * Ensure database connection works using environment variables

4. Fix folder structure:

backend/
src/
routes/
controllers/
models/
middleware/
config/
package.json

frontend/
src/
package.json

5. Fix all import paths:

   * Example:
     "@workspace/db" → "../lib/db"
   * Ensure all modules resolve correctly

6. Fix package.json:

   * Remove invalid JSON
   * Remove unsupported dependencies
   * Add only required dependencies:
     express, cors, dotenv, mongoose (or drizzle), jsonwebtoken, bcryptjs

7. Fix scripts:

   * Make sure this works on Windows:
     "dev": "set NODE_ENV=development && tsx src/index.ts"

8. Ensure project runs locally:

   * Backend runs on http://localhost:5000
   * Frontend runs on http://localhost:5173
   * No runtime errors

9. Remove unnecessary files:

   * Remove mockup-sandbox
   * Remove unused build scripts
   * Clean up unused code

10. Add .env support:

* MONGO_URI
* JWT_SECRET
* PORT

---

FINAL OUTPUT:

* Clean and working backend
* Clean and working frontend
* No Replit-specific code
* Fully runnable in VS Code
* Minimal and maintainable structure

---

Do NOT overcomplicate the project.
Keep it simple, clean, and production-ready.

Ensure everything runs without errors.
